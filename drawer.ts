let offsetX = 0;
let offsetY = 0;

const fontSize = 20;
const canvasWidth = 500;
const canvasHeight = 500;

function _x(x: number): number {
  return x + offsetX;
}

function _y(y: number): number {
  return y + offsetY;
}
